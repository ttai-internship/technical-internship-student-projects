# B0-04：函数和最小测试

## 目标

把一段可重复的操作写成函数，并用断言确认行为。

## 操作

1. 先阅读 lesson04 的完整示例；
2. 预测函数在 0 次、1 次和 3 次调用时的结果；
3. 修改一个参数；
4. 为自己的修改增加一个断言；
5. 记录一个失败断言和修复过程。

## 验收

- 能指出参数、返回值和局部变量；
- 至少三个断言通过；
- 能用一句话解释测试失败的原因。
