# B0-01：运行程序并修改输出

## 目标

建立“文件、命令、输出”之间的最小联系。

## 操作

1. 运行 starter 中的 lesson01；
2. 预测 greeting("Ada") 的返回值；
3. 把默认问候语改成自己的版本；
4. 再运行一次并保存命令和输出。

## 验收

- 能从项目目录运行 Python 文件；
- 能解释字符串为什么需要引号；
- 能提交一次只改变输出的 commit。
