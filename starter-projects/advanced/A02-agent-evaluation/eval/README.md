# A02 fixed evaluation set

This is a small offline trace set. It intentionally requires no API key and
does not send prompts or data to an external service. The cases are fixed so
that changes to a metric, classifier or prompt policy can be compared on the
same evidence.

Students may add a second metric or a second candidate answer set, but the
original case order and baseline result must remain available.
